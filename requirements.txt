# Core LangGraph / LangChain stack
# Pinned to reasonably recent minors; loosened patch to survive Python 3.14 wheel availability.
langgraph>=0.2.60
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-core>=0.3.0

# Config + validation
pydantic>=2.7
python-dotenv>=1.0

# PokéAPI HTTP client
httpx>=0.27

# HTTP transport for the chat UI (streaming endpoint)
fastapi>=0.115
uvicorn>=0.30

# Optional: tracing (LangSmith). Harmless if unused; enable via env.
# langsmith>=0.1
